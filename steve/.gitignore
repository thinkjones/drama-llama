# Gradle / Forge build artifacts — regenerated by ./gradlew, never commit these.
.gradle/
build/
run/
out/
bin/

# Forge installer logs (e.g. from `steve forge` / running the installer jar directly)
forge-*-installer.jar.log

# IDE files
.idea/
*.iml
.eclipse/
.settings/
.classpath
.project
